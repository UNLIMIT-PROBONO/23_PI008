const SERVER_HOST = "localhost";
const SERVER_PORT = "8080";
const BASE_URL = `http://${SERVER_HOST}:${SERVER_PORT}/api/`;


// rest api URI 관리
export const URI = {
    LOGIN: `/login`,
    SIGNUP: `/signup`,
    CHECK_ID: `/signup/id`,
};

// 페이지 라우팅 URI 관리
export const ROUTE = {

};